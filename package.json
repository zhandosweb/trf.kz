{
  "scripts": {
    "dev": "rmdir /s /q public && hugo server",
    "build": "hugo --cleanDestinationDir"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.16",
    "@tailwindcss/typography": "^0.5.19",
    "tailwindcss": "^4.1.16"
  }
}
